[package]
name = "toru_os"
version = "0.1.0"
edition = "2024"
default-run = "toru_os"

[workspace]
members = ["kernel", "disk-builder"]

[dependencies]
disk-builder = { path = "disk-builder" }
ovmf-prebuilt = "0.1.0-alpha"

[build-dependencies]
disk-builder = { path = "disk-builder" }
